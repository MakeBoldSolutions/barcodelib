@model Barcode.Web.Models.BarCodeVM
@{
    ViewData["Title"] = "BarCode";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <!-- List group-->
            <ul class="list-group shadow">
                @foreach (var item in Model.BarCodeList)
                {
                    <!-- list group item-->
                    <li class="list-group-item">
                        <!-- Custom content-->
                        <div class="media align-items-lg-center flex-column flex-lg-row p-3">
                            <div class="media-body order-2 order-lg-1">
                                <h5 class="mt-0 font-weight-bold mb-2">
                                    <a asp-action="Details" asp-route-id="@item.Id">@item.AlternateLabel</a>
                                </h5>
                            </div>
                            <img src="data:image;base64,@System.Convert.ToBase64String(item.BarcodeByteArray)" class="img-fluid ml-lg-5 order-1 order-lg-2" />
                        </div>
                        <!-- End -->
                    </li>
                }
            </ul>
            <!-- End -->
        </div>
    </div>
</div>
